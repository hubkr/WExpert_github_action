<Page
    x:Class="WExpert.Views.SplashPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:behaviors="using:WExpert.Binding.Behavior"
    xmlns:wexcontrols="using:WExpert.Controls"
    xmlns:converter="using:WExpert.Binding.Converter"
    xmlns:helper="using:WExpert.Helpers"
    Background="{StaticResource WGrey900}"
    Loaded="Page_Loaded">

    <Page.Resources>
        <Storyboard x:Name="FadeAnimation">
            <DoubleAnimation Storyboard.TargetName="Logo"
                                 Storyboard.TargetProperty="Opacity"
                                 From="0.0" To="1.0"
                                 Duration="0:0:1.5"
                                 AutoReverse="True"
                                 RepeatBehavior="Forever"/>
        </Storyboard>

        <!-- #region TextBlock Style -->
        <Style x:Key="SplashPageLocalTextBlockStyle1" TargetType="TextBlock"
               BasedOn="{StaticResource TextBlockStyleBase}">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="LineHeight" Value="21" />
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{StaticResource TextTertiary}" />
        </Style>
        <!-- #endregion TextBlock Style -->
    </Page.Resources>

    <Grid x:Name="ContentArea" 
          Width="252"
          HorizontalAlignment="Center"
          VerticalAlignment="Center">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Image x:Name="Logo"
                   Width="252"
                   Height="54"
                   Source="ms-appx:///Assets/Images/logo.png"
                   Opacity="0"/>
        </Grid>

        <Grid Grid.Row="2">
            <ProgressBar Value="{x:Bind ViewModel.ProgressPercentage, Mode=OneWay}"
                         Foreground="{StaticResource ComponentsBlue}" />
        </Grid>

        <Grid Grid.Row="4">
            <TextBlock Text="{x:Bind ViewModel.ProgressText, Mode=OneWay}"
                       Style="{StaticResource SplashPageLocalTextBlockStyle1}" />
        </Grid>

    </Grid>
</Page>